apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.php.serviceName }}
spec:
  type: ClusterIP
  selector:
    app: php
  ports:
    - port: {{ .Values.php.port }}
      targetPort: {{ .Values.php.targetPort }}